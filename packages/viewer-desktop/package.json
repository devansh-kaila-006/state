{
  "name": "@state/viewer-desktop",
  "version": "0.1.0",
  "description": "Desktop viewer for .agent files (Tauri)",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "icon": "tauri icon",
    "info": "tauri info"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0",
    "typescript": "^5.3.3"
  }
}
